<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
	<record id="assigned_ticket_reminder_request_email_template" model="mail.template">
        <field name="name">Ticket: Assigned Reminder</field>
        <field name="model_id" ref="helpdesk.model_helpdesk_ticket"/>
        <field name="subject">Reminder (${object.display_name})</field>
        <field name="email_from">${object.user_id.email_formatted | safe}</field>
        <field name="email_to">${('' if object.user_id.partner_id and object.sudo().user_id.partner_id.email | safe)}</field>
        <field name="partner_to">${object.user_id.partner_id and object.sudo().user_id.partner_id.email and object.sudo().user_id.partner_id.id or False}</field>
        <field name="lang">${object.user_id.partner_id.lang or object.user_id.lang or user.lang | safe}</field>
        <field name="body_html" type="xml">
	    <div>
		Dear ${object.sudo().user_id.partner_id.name or 'Madam/Sir'},<br /><br />
		The request
		% if object.get_portal_url():
		<a href="/helpdesk/ticket/${object.id}/${object.access_token}">${object.name}</a>
		% endif
		assigned to you is awaiting processing, with a time from your assignment of <strong>${object.last_assign_hours} Hours.</strong><br />
		Thanks you,<br /><br />
	    </div>
        </field>
        <field name="auto_delete" eval="False"/>
    </record>
    </data>
</odoo>
